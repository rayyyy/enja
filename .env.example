# macOS code signing (optional, for stable TCC permissions across upgrades)
# Run `security find-identity -v -p codesigning` to find your identity.
# Without this, the app will be ad-hoc signed and macOS may require
# re-granting Accessibility / Input Monitoring permissions after each update.
APPLE_SIGNING_IDENTITY="Apple Development: Your Name (XXXXXXXXXX)"
